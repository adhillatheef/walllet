import 'package:flutter/material.dart';

class AppColors {
  // Main Backgrounds
  static const Color scaffoldBackground = Color(0xFF0B0B0B); // Almost black
  static const Color cardSurface = Color(0xFF161616); // Slightly lighter for cards
  static const Color inputBackground = Color(0xFF232323); // Search bars/Inputs

  // Brand & Accents
  static const Color primaryCyan = Color(0xFF55E0FF); // The "W" logo color
  static const Color primaryPurple = Color(0xFF9D59FF); // Gradient chart accents

  // Functional Colors
  static const Color successGreen = Color(0xFF00D180); // +$200.00
  static const Color errorRed = Color(0xFFFF4D4D);    // -$200.00, Rejected
  static const Color warningOrange = Color(0xFFFFAB2E); // Bonus/Pending

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E8E93); // Subtitles/Labels
  static const Color textHint = Color(0xFF505050);

  // UI Elements
  static const Color divider = Color(0xFF2C2C2C);
  static const Color whiteBtn = Color(0xFFFFFFFF);
}